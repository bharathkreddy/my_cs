# Env setup
```
1. uv init
2. uv venv
3. source .venv/bin/activate
4. uv add ipykernel
5. python -m ipykernel install --user --name=llm-twin --display-name "Python (llm-twin)"
6. uv run --with jupyter jupyter lab
```

